var _user$project$Native_Random = (function () {
    /**
     * Return random float (0,1)
     */
    var rand = function () {
        return Math.random()
    }

    return {
        rand: rand
    }
})()

