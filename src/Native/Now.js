var _user$project$Native_Now = (function () {
    /**
     * Return current timestamp in miniseconds
     */
    var now = function () {
        return new Date()/1
    }

    return {
        now: now()
    }
})()
